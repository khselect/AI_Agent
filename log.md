# 변경 이력 (Safety Agent)

---

## v1.7.0 — 2026-05-20 · 모델명 오타 수정 및 문서화

### [문제점]
- 전체 코드에 모델명이 `qwen3:32`로 잘못 등록 (올바른 이름: `qwen3:32b`)
- Ollama에서 404 Not Found 발생 → `except Exception`에 조용히 흡수
- AI 기여 0개 + 정규식만 추출 → 약 20/43 (47%) 추출률
- 오류가 사용자에게 표시되지 않아 원인 파악 어려움

### [개선점]
- `qwen3:32` → `qwen3:32b` 전 파일 일괄 수정
  (safety_analytics.py, safety_core.py, railway_safety_agent.py, agent_ui.py, CLAUDE.md — 총 10곳)
- 모델 404/미발견 오류 시 화면에 명시적 메시지 표시
- 기술상세서(`docs/기술상세서.md`) 신규 작성 (비전공자용 시스템 설명서)
- CLAUDE.md 전면 갱신 (PDF 파이프라인 실제 동작 기준으로 재작성)
- 수정 후 추출률: **95% 이상** (qwen3:32b 기준)

---

## v1.6.0 — 2026-05-20 · 추출 파이프라인 전면 재설계 (정확도 우선)

### [문제점]
- LLM이 0개 필드를 기여 → 20/43 전부 regex 단독 추출이었음
- `format="json"` 옵션: Ollama가 JSON 문법 제약을 토큰 레벨에서 강제 →
  한국어 텍스트 값 생성 차단, 모든 텍스트 필드 null 반환
- 5개 배치 중 배치0~3가 여전히 부분 텍스트 사용 → 필드 위치에 따라 누락
- 이벤트개요는 요약 생성 태스크인데 일반 추출 프롬프트로 처리 → 항상 실패
- 프롬프트가 영어로 작성되어 한국어 보고서와 필드명 매핑 부정확

### [개선점]
- `format="json"` 완전 제거 — 한국어 텍스트 생성 차단 원인 제거
- 모든 배치 전체 보고서 텍스트 사용 (28000자, 정확도 우선)
- 프롬프트 전면 한국어 재작성 (시스템 메시지·배치 프롬프트 모두)
- 고장 4개 필드 전용 재추출 패스 추가 ('고장','이상','불량','조치' 키워드 안내)
- 이벤트개요 전용 요약 생성 패스 분리 (추출→생성 태스크로 변환)
- 이벤트개요 합성 fallback: LLM 실패 시 기추출 필드로 자동 문장 조합
- 수정 파일: `safety_analytics.py`, `safety_core.py`

---

## v1.5.0 — 2026-05-20 · 배치4 누락 필드 추출률 개선

### [문제점]
- 고장부품명·고장현상·고장원인·조치내용·이벤트개요 5개 필드 전부 미추출
- `BATCH_SLICE[4] = (8000, None)[:12000]` → 문서 8000~20000자 구간만 읽음
  (고장·조치 내용이 앞부분에 있으면 완전 누락)
- 모든 배치 텍스트 상한 12000자 → 긴 보고서 후반부 차단
- 이벤트개요는 전체 보고서 요약이 필요한 필드인데 일반 슬라이스로 처리

### [개선점]
- `BATCH_SLICE[4]` → `(0, None)` : 전체 보고서 처음부터 읽도록 변경
- 텍스트 상한: 일반 배치 12000→16000자, 배치4 전용 28000자 적용
- 배치4 프롬프트에 한국어 강조 지시 추가 (고장·조치·개요 5개 필드 명시)
- 5개 배치 완료 후 누락 필드 대상 보완 추출 패스 추가 (전체 텍스트 28000자 사용)
- 이벤트개요 최종 합성 fallback: LLM 실패 시 추출된 필드로 자동 문장 조합
- 수정 파일: `safety_analytics.py`, `safety_core.py`

---

## v1.4.0 — 2026-05-20 · qwen3 추출 정확도 개선

### [문제점]
- `qwen3:32b` 모델 선택 시 `qwen2.5:7b-instruct` 대비 43개 필드 추출률 저하
- `/no_think` 를 프롬프트 텍스트에 삽입하는 방식으로는 thinking 모드가 실제로 비활성화되지 않음
- thinking 모드 ON 상태에서 `<think>` 블록이 `num_predict=2048` 토큰 대부분을 소비해 JSON 출력 잘림
- 배치 파싱 실패 시 재시도 없이 조용히 skip → 필드 누락
- `format="json"` 미적용으로 마크다운/설명문 섞인 응답 파싱 오류 발생

### [개선점]
- `reasoning=False` 파라미터 추가 (`langchain_ollama 0.3+` → Ollama `think=false` 전달) — thinking 완전 비활성화
- `num_predict` 2048 → 4096 으로 증가 — JSON 출력 잘림 방지
- `format="json"` 옵션 추가 — 모델이 JSON 구조만 출력하도록 강제
- 배치별 실패 시 1회 재시도 로직 추가 — 파싱 오류에 의한 누락 방지
- 프롬프트에서 불필요한 `/no_think` prefix 제거 (reasoning=False 로 대체)
- 수정 파일: `safety_analytics.py`, `safety_core.py`
- 메인 페이지 상단에 버전/커밋 정보 표시 추가

---

## v1.3.0 — 2026-03-29 · LLM 추출 버그 및 DB 컬럼명 불일치 수정

### [문제점]
- LLM 추출 비활성화 버그 (조건 분기 오류)
- DB 컬럼명 불일치 (`최대지연시간_분` vs `최대지연시간(분)`) 로 INSERT 실패

### [개선점]
- LLM 활성화 조건 분기 수정
- DB 컬럼명 `"최대지연시간(분)"`, `"피해액(백만원)"` 특수문자 포함 형태로 통일

---

## v1.2.0 — 2026-03-29 · 3-레이어 아키텍처 리팩토링

### [문제점]
- `safety_analytics.py` 단일 모놀리식 파일에 UI·비즈니스로직·DB·에이전트 혼재
- 에이전트(`railway_safety_agent.py`)가 Streamlit을 간접 import해 독립 실행 불가

### [개선점]
- `safety_core.py` 분리 (비즈니스 로직, Streamlit 의존 없음)
- UI 탭별 모듈화 (`ui/tab_*.py`)
- 에이전트가 `safety_core` 만 import하도록 변경

---

## v1.1.0 — CLAUDE.md·.gitignore 추가

### [개선점]
- `CLAUDE.md` 작성 (아키텍처·DB 스키마·실행방법 문서화)
- `.gitignore` 추가 (`.venv/`, `__pycache__/`, `*.pyc` 제외)
- `notify_config.json` 알림 이력 구조 정비
